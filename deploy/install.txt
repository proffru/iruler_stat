pip freeze > requirements.txt

python.exe -m pip install --upgrade pip
pip install wheel
pip install django
pip install Pillow
pip install django-phonenumber-field[phonenumbers]
pip install uuid

pip install wheel django Pillow django-phonenumber-field[phonenumbers]

pip install daphne
pip install psycopg2-binary
pip install gunicorn

pip install daphne psycopg2-binary gunicorn

pip install djangorestframework
pip install django-otp
pip install qrcode
pip install requests
pip install python-dotenv
pip install Celery
pip install redis
pip install celery[redis]
pip install django-celery-beat
pip install django-debug-toolbar

pip install drf-yasg
pip install flower
pip install openpyxl

pip install drf-yasg daphne flower
pip install wheel django Pillow django-phonenumber-field[phonenumbers] daphne psycopg2-binary djangorestframework gunicorn
pip install django-otp qrcode requests python-dotenv Celery redis celery[redis] django-celery-beat django-debug-toolbar
pip install uuid openpyxl

pip install pandas
